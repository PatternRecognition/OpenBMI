This example program was compiled with gcc using the MinGW runtime using "gcc example.c TomographyAPI.lib -o apitest.exe".  Other compilers should be able to build it using their 
own methodologies.

It is invoked from the command line as 'apitest {address} {port} {timeout}' (where timeout is in milliseconds.